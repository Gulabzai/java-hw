package class5;

public class E5LogicalOperators {
    public static void main(String[] args) {
        /*
        NOT ! it flips the boolean values for example if the value is true it will change it to false and if it is false
        it will change to true
        and also it works only with boolean data type
         */
        boolean flag=true;
        System.out.println(!flag);
        System.out.println(flag);

        String name="Samir";
        System.out.println("!Samir");//so it is wirk with boolean values
    }
}
